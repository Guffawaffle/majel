# PM Sweep — 2026-03-11

## Summary

ADR-042 (Model Availability Policy) all 3 phases shipped. ADR-041 umbrella (#199) and ADR-042 umbrella (#205) closed. Backlog reconciled against open issues. Test count: 2120 across 78 files.

## Shipped Since Last Sweep

### ADR-042 Phase 1 — Registry + Centralized Resolver (`7612404`, #206)
- `defaultEnabled` field on all `ModelDef` entries (stable Gemini = true, preview + Claude = false)
- `resolveModelAvailability()` pure function composing 4 signals: registry default, provider capability, role gate, admin override
- `system.modelOverrides` JSON blob in SettingsStore
- Replaced inline provider checks in `GET /api/models`, `POST /api/models/select`
- 22 unit tests for resolver

### ADR-042 Phase 2 — Admin Endpoints + UI (`c0a4b45`, #207)
- `GET /api/admiral/models` — full model list with availability breakdown
- `PATCH /api/admiral/models/:id/availability` — toggle admin override with optional reason
- Models tab in AdmiralView with enable/disable/reset controls and provider badges
- Frontend types: `AdminModelEntry`, `AdminModelsResponse`, `AdminModelToggleResponse`
- Frontend API: `adminListModels()`, `adminSetModelAvailability()` with mutation locking
- 11 new tests in `admiral-routes.test.ts`

### ADR-042 Phase 3 — Frontend Picker Polish (`4f6355c`, #208)
- Model picker shows unavailable models greyed out (opacity 0.4, cursor: not-allowed)
- Tooltip with unavailableReason on hover
- Inline reason text in model card meta row
- Selection guard — clicking unavailable model shows system message instead of switch dialog
- Backend POST /api/models/select already rejects unavailable models as defense-in-depth

## Issue Reconciliation

| Issue | Title | Action | Reason |
|---|---|---|---|
| #199 | ADR-041 umbrella | **Closed** | All 5 phases shipped, DoD met |
| #205 | ADR-042 umbrella | **Closed** | All 3 phases shipped |
| #206 | ADR-042 Phase 1 | Closed | Shipped `7612404` |
| #207 | ADR-042 Phase 2 | Closed | Shipped `c0a4b45` |
| #208 | ADR-042 Phase 3 | **Closed** | Shipped `4f6355c` |
| #209–#211 | ADR-043 | Open | Not started — next program after ADR-042 |
| #189–#193 | Tech debt batch | Open | 5 refactor issues, not blocking features |
| #161, #165 | QA tranche | Open | Not started |
| #131 | Effects taxonomy | Open | Phase C in progress |
| #72 | Data pipeline | Open | Phase 3 in progress |
| #65 | Startup/Import UX | Open | Not started |
| #10 | Image interpretation | Open | Phase A done, B/C future |

## Open Issue Count

- **Before sweep:** 17 open
- **After sweep:** 14 open (closed #199, #205, #208)
- **Recently closed:** #199 (ADR-041 umbrella), #205 (ADR-042 umbrella), #206 (P1), #207 (P2), #208 (P3)

## Backlog Updates

- BACKLOG.md "Current PM Focus" updated with current state
- ADR-042 program table: Phase 1+2 marked `[x] Done` with commit hashes
- ADR-042 DoD: 5/5 items checked — program complete
- Test count updated: 2120 tests, 78 files

## Active Programs

| Program | Status | Next Phase |
|---|---|---|
| ADR-042 Model Availability | **Complete** | All 3 phases shipped, umbrella closed |
| ADR-043 Chat Run Control | Not started | Phase 1: Stop button + run state (#210) |
| ADR-034 Effects Taxonomy | Phase C in progress | Crew validator (#134) |

## Blockers & Risks

- **Claude quota:** Still denied. No billing history on $300 GCP credit account. Will re-request after billing established. Gemini-only in production.
- **No blockers** on ADR-042 Phase 3 or ADR-043.

## Validation

- `npm run ax -- ci` ✅ (2120/2120 tests, 0 failures, 7 pipeline steps)
- Tree clean, `main` pushed to `28b7b95`
- Cloud deployed successfully
