# Majel Environment Configuration
# Copy to .env and fill in values

# === Google Gemini (Required) ===
GEMINI_API_KEY=your-gemini-api-key-here

# === Server ===
MAJEL_PORT=3000

# === Isolation ===
# Ensures Majel uses its own Lex database, not your global one
LEX_WORKSPACE_ROOT=/srv/majel

# === Database (Phase 3+: PostgreSQL) ===
# Local dev: use docker compose up -d postgres, then:
DATABASE_URL=postgres://majel:majel@localhost:5432/majel
# Cloud Run: set via Cloud SQL Auth Proxy (unix socket), e.g.:
# DATABASE_URL=postgres:///majel?host=/cloudsql/PROJECT:REGION:INSTANCE

# === Auth (Phase 2+: Tiered access) ===
MAJEL_ADMIN_TOKEN=
MAJEL_INVITE_SECRET=
