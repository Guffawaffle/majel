# Majel Environment Configuration
# Copy to .env and fill in values

# === Google Gemini (Required) ===
GEMINI_API_KEY=your-gemini-api-key-here

# === Server ===
MAJEL_PORT=3000

# === Isolation ===
# Ensures Majel uses its own Lex database, not your global one
LEX_WORKSPACE_ROOT=/srv/majel

# === Turso (Phase 1+: Cloud database) ===
# Leave blank for local dev (uses file: SQLite databases)
TURSO_REFERENCE_URL=
TURSO_REFERENCE_TOKEN=
TURSO_ADMIN_URL=
TURSO_ADMIN_TOKEN=
TURSO_TENANT_TEST_URL=
TURSO_TENANT_TEST_TOKEN=

# === Auth (Phase 2+: Tiered access) ===
MAJEL_ADMIN_TOKEN=
MAJEL_INVITE_SECRET=
