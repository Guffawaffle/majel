{
  "name": "majel",
  "version": "0.4.0",
  "description": "STFC Fleet Intelligence System — Named in honor of Majel Barrett-Roddenberry (1932–2008)",
  "type": "module",
  "main": "dist/server/index.js",
  "scripts": {
    "build": "tsc && cp -r src/client dist/client",
    "start": "node --import dotenv/config dist/server/index.js",
    "dev": "MAJEL_LOG_PRETTY=true tsx watch --import dotenv/config src/server/index.ts",
    "dev:full": "npm run pg:start && npm run dev",
    "dev:fresh": "npm run kill --silent; npm run dev",
    "dev:wipe": "rm -rf .smartergpt/lex/reference.db .smartergpt/lex/reference.db-wal .smartergpt/lex/reference.db-shm && echo '🗑️ reference.db wiped (overlay + dock data cleared)'",
    "dev:bg": "npm run kill --silent; mkdir -p logs && MAJEL_LOG_PRETTY=true npm run dev > logs/dev.log 2>&1 & echo $! > .dev.pid && echo 'Majel running in background (PID: '$(cat .dev.pid)')' && echo '  Logs: npm run dev:log'",
    "dev:log": "tail -f logs/dev.log",
    "dev:stop": "npm run kill --silent; rm -f .dev.pid; echo 'Majel stopped'",
    "kill": "lsof -t -i:${MAJEL_PORT:-3000} | xargs -r kill -9 2>/dev/null; echo '🔪 Port ${MAJEL_PORT:-3000} cleared'",
    "clean": "rm -rf dist coverage .smartergpt/lex/settings.db .smartergpt/lex/chat.db .test-sessions.db",
    "rebuild": "npm run clean && npm run build",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/check-csp-hash.mjs && eslint .",
    "lint:fix": "eslint . --fix",
    "check:csp": "node scripts/check-csp-hash.mjs",
    "fix:csp": "node scripts/check-csp-hash.mjs --fix",
    "test": "[ \"$CI\" = true ] || npm run pg:check --silent && vitest run",
    "test:watch": "[ \"$CI\" = true ] || npm run pg:check --silent && vitest",
    "test:coverage": "vitest run --coverage",
    "local-ci": "npm run lint && npm run typecheck && npm run test:coverage && npm run build",
    "promote": "tsx scripts/promote.ts",
    "health": "curl -s http://localhost:${MAJEL_PORT:-3000}/api/health | python3 -m json.tool",
    "cloud": "tsx scripts/cloud.ts help",
    "cloud:deploy": "tsx scripts/cloud.ts deploy",
    "cloud:build": "tsx scripts/cloud.ts build",
    "cloud:push": "tsx scripts/cloud.ts push",
    "cloud:logs": "tsx scripts/cloud.ts logs",
    "cloud:status": "tsx scripts/cloud.ts status",
    "cloud:health": "tsx scripts/cloud.ts health",
    "cloud:secrets": "tsx scripts/cloud.ts secrets",
    "cloud:ssh": "tsx scripts/cloud.ts ssh",
    "cloud:rollback": "tsx scripts/cloud.ts rollback",
    "cloud:env": "tsx scripts/cloud.ts env",
    "cloud:init": "tsx scripts/cloud.ts init",
    "cloud:sql": "tsx scripts/cloud.ts sql",
    "cloud:revisions": "tsx scripts/cloud.ts revisions",
    "cloud:scale": "tsx scripts/cloud.ts scale",
    "cloud:canary": "tsx scripts/cloud.ts canary",
    "cloud:promote": "tsx scripts/cloud.ts promote",
    "cloud:diff": "tsx scripts/cloud.ts diff",
    "cloud:metrics": "tsx scripts/cloud.ts metrics",
    "cloud:costs": "tsx scripts/cloud.ts costs",
    "cloud:warm": "tsx scripts/cloud.ts warm",
    "pg:check": "docker exec majel-postgres-1 pg_isready -U majel -q 2>/dev/null && echo '✅ PostgreSQL is running' || (echo '❌ PostgreSQL is NOT running — run: npm run pg:start' && exit 1)",
    "pg:start": "docker start majel-postgres-1 2>/dev/null || docker compose up -d postgres; echo '⏳ Waiting for PostgreSQL...'; for i in $(seq 1 30); do docker exec majel-postgres-1 pg_isready -U majel -q 2>/dev/null && echo '✅ PostgreSQL ready' && exit 0; sleep 1; done; echo '❌ PostgreSQL failed to start within 30s' && exit 1",
    "pg:stop": "docker stop majel-postgres-1 && echo '🛑 PostgreSQL stopped'",
    "gh": "node scripts/gh.mjs"
  },
  "keywords": [
    "stfc",
    "fleet",
    "gemini",
    "lex"
  ],
  "author": "Admiral Guff",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@smartergpt/lex": "^2.5.0",
    "argon2": "^0.44.0",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "express-rate-limit": "^8.2.1",
    "pg": "^8.18.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/compression": "^1.8.1",
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^4.17.25",
    "@types/express-rate-limit": "^5.1.3",
    "@types/node": "^20.19.33",
    "@types/pg": "^8.16.0",
    "@types/pino-http": "^5.8.4",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^4.0.18",
    "eslint": "^9.39.2",
    "globals": "^17.3.0",
    "pino-pretty": "^13.1.3",
    "supertest": "^7.2.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.55.0",
    "vitest": "^4.0.18"
  }
}
