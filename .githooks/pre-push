#!/usr/bin/env bash
# .githooks/pre-push — Strict local pre-push gate for Majel
#
# Runs the full AX CI suite locally and blocks pushes on any gate failure.
# Install: git config core.hooksPath .githooks

set -euo pipefail

echo "⟐ pre-push: ax ci"
npm run ax -- ci

echo "✅ pre-push passed"
