<script lang="ts">
  /** Workshop view — Composition workshop, cores, loadouts, policies (placeholder) */
</script>

<div class="view-placeholder">
  <span class="view-placeholder-icon">⚓</span>
  <h2>Workshop</h2>
  <p>Composition workshop — cores, loadouts, policies & reservations — coming in Phase 5</p>
</div>

<style>
  .view-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    flex: 1;
    gap: 12px;
    color: var(--text-muted);
  }
  .view-placeholder-icon { font-size: 3rem; }
  h2 { color: var(--accent-gold); font-size: 1.4rem; }
  p { font-size: 0.9rem; }
</style>
